<header>
    <nav class="navbar">
        <a href="/" class="logo">Fresh Fruits</a>
        <div class="nav-links">
            <% if (locals.user) { %>
                <span>Welcome, <%= user.username %></span>
                <a href="/auth/logout">Logout</a>
            <% } else { %>
                <a href="/auth/login">Login</a>
                <a href="/auth/register">Register</a>
            <% } %>
        </div>
    </nav>
</header>